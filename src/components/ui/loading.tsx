'use client';

const Loading = () => {
    return (
        <span className="inline-block size-5 animate-spin rounded-full border-2 border-t-transparent border-gray-300" />
      );
}

export default Loading;